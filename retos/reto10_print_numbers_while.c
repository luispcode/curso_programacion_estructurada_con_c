#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

int main()
{
    int fila;

   //inicializa 
    fila = 0
    /* condicion while */ 
    while (fila < 5)
    //hacer esto e incrementar
    {
        printf("# \n");
        fila++;
    }

    //continua

    printf("\nfin");
}
